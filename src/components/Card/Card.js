import React from "react";

const Card = ({ image, title }) => {
  return (
    <div>
      <img src="" />
      <p>DESIGN PORTFOLIO</p>
      <div>
        <span>HTML</span>
        <span> CSS</span>
      </div>
    </div>
  );
};

export default Card;
